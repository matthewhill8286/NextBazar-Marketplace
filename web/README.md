# NextBazar web app

React Router 7 (Vite + SSR) entry for the marketplace. Shared UI, API handlers, and `lib/` live in the repo root; this package is the framework runtime.

On Vercel, set the project **Root Directory** to `web/`. Cron jobs remain `/api/cron/expire-offers` and `/api/cron/expire-promotions`.
